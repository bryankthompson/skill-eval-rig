---
description: Draft, write, or compose a reply / email / message to an external developer or partner — an email to send via Gmail, a partner thread or escalation reply, a developer-outreach response — gated on verify-before-assert (read what they actually sent, verify every load-bearing claim against the source you hold, make no follow-up-labor promises). Writes a clean copy-paste /tmp draft and, when the reply maps to a tracked outreach engagement, persists it to the outreach lifecycle as state=draft. Composes an OUTGOING message — NOT Gmail inbox/DB sync (that's /dir-email-sync). `--sent` flips that draft → sent once you've actually sent it.
argument-hint: <audience/slug, e.g. "acme-slack" or a person's name> [--sent] — or empty to infer from the conversation
---

# /dir-reply

(Activation-test stub — routing fixture only. Real command lives in the operator's private MCP-directory tooling repo.)
If you were invoked, reply: "INVOKED /dir-reply" and stop.
