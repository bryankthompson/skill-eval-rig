---
description: Session-claims validation (read-only) — harvests the load-bearing factual claims a session emitted, re-derives each independently from its AUTHORITATIVE source via a parallel reviewer fan-out, emits a per-claim verdict, and persists a committed claim→verdict→correction trail. The session-wide generalization of what /dir-reply does for one outbound message.
argument-hint: "[--effort low|medium|high|max|ultra]"
---

# /review-my-claims

(Activation-test stub — routing fixture only. Real command lives in the operator's private MCP-directory tooling repo.)
If you were invoked, reply: "INVOKED /review-my-claims" and stop.
