---
description: Verified developer outreach for audited MCP servers — verify findings, draft GitHub issues, post with approval, check issue status
argument-hint: <slug, "<slug> --email", "<slug> --email-sent", "status", "status refresh", "status json", or empty for auto-queue>
---

# /dir-outreach

(Activation-test stub — routing fixture only. Real command lives in mcp-local-directory.)
If you were invoked, reply: "INVOKED /dir-outreach" and stop.
