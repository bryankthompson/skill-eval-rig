---
description: Draft an external/partner reply gated on verify-before-assert — read the artifact the human/client actually sent, enumerate every load-bearing claim against the source you touched, write a clean copy-paste draft to /tmp, AND (when it maps to a tracked outreach engagement) persist it to the outreach lifecycle as state=draft so it's queued + visible on the dashboard. `--sent` flips that draft → sent once you've actually sent it.
argument-hint: <audience/slug, e.g. "acme-slack" or a person's name> [--sent] — or empty to infer from the conversation
---

# /dir-reply

(Activation-test stub — OLD (original, pre-revision) description. Routing fixture only.)
If you were invoked, reply: "INVOKED /dir-reply" and stop.
