---
description: Read-only cross-surface status for a single MCP server — fans out across registry, staging, outreach, asana-db, the submission sheet, ~/mcp-logs audits, GitHub issues, and GDrive bundles; produces a paste-ready summary
argument-hint: <slug | repo | github-url | dev-email> [--paste] [--json] [--all] [--contact]
---

# /dir-server-status

(Activation-test stub — routing fixture only. Real command lives in mcp-local-directory.)
If you were invoked, reply: "INVOKED /dir-server-status" and stop.
