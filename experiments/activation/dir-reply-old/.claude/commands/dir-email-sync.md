---
description: Sync email tracking DB with Gmail — detect new replies, update statuses, flag stale threads
argument-hint: <empty for full sync, "org-name" to sync one org, "dry-run" to preview, "--discover [--since=1y]" to find untracked threads>
---

# /dir-email-sync

(Activation-test stub — routing fixture only. Real command lives in the operator's private MCP-directory tooling repo.)
If you were invoked, reply: "INVOKED /dir-email-sync" and stop.
