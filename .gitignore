output/
*.debug
.DS_Store
__pycache__/
.venv/
# trial outputs + per-trial stderr; never commit a clone's real-corpus transcripts (N3)
_trials/
*.jsonl
.claude/settings.json
# per-clone VS Code launcher generated by experiments/activation/install-vscode-tasks.sh;
# source of truth is experiments/activation/tasks.json. Narrow this if you ever add an
# intentional .vscode/settings.json (then ignore only .vscode/tasks.json).
.vscode/

# local-only deny tokens for the public-hygiene guard (never publish)
tests/.deny-local
