# Driver-only dependency. Used solely by experiments/activation/drive_interactive.py to drive
# INTERACTIVE `claude` sessions in a pty (the auto-activation axis). The headless axes
# (run_trials.sh/score.py/gen_*.py) and the test suite (`make test`) run on bare python3 and do
# NOT import pexpect — so CI stays green without installing this.
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#   bash experiments/activation.sh        # the wrapper auto-uses .venv/bin/python3
pexpect>=4.9
